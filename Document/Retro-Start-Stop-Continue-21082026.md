Weekly Log

IsoWeekNum: W34

TinTDD



|Start|Stop|Continue|
|-|-|-|
|Đọc đầy đủ các dòng bị grep rút gọn ("...omitted...") trước khi kết luận là an toàn|Đoán mù các chỉnh sửa hình ảnh (màu, bo góc, shadow) nhiều vòng liên tiếp mà không đo pixel thực tế trước|Viết và chạy lại test headless (dom-stub + vm) sau mỗi lần sửa lớn để bắt lỗi trước khi test tay trên browser|
|Quét toàn file tìm hết mọi nơi tham chiếu tới 1 id/hàm trước khi xoá hoặc sửa nó|Cho rằng 1 hàm chỉ có 1 định nghĩa, trong khi file có nhiều lớp patch ghi đè cùng tên (renderHUD, startLevel, showPreview...)|Backup file gốc trước khi merge lớn, vì project chưa có git để lùi lại|
|Xin log console/devtools của người dùng ngay khi nghi ngờ có lỗi runtime mà môi trường giả lập không tái hiện được|Thêm UI mới (badge, hint) mà không kiểm tra nó có che khuất control tương tác bên dưới (z-index, pointer-events)|Dùng script có assertion đếm số lần thay thế khi sửa hàng loạt text/CSS, để không sửa nhầm hoặc sửa thiếu|
|Hỏi lại phạm vi cụ thể trước khi sửa khi yêu cầu còn mơ hồ|Xoá markup/phần tử chỉ dựa trên 1 lượt grep hẹp mà không double-check toàn diện|Tối giản dần UI/text theo phản hồi lặp lại của người dùng, xác nhận lại bằng câu hỏi ngắn khi cần|



